const api = globalThis.browser || globalThis.chrome, DEFAULTS = { method: "builtin", provider: "dave", endpoint: "https://davefrassoni.com", apiKeys: {}, model: "" };
const $ = selector => document.querySelector(selector);
async function load() { const data = await api.storage.local.get({ organizerSettings: DEFAULTS }); const value = { ...DEFAULTS, ...data.organizerSettings }; $("#method").value = value.method; $("#provider").value = value.provider; $("#endpoint").value = value.endpoint; $("#model").value = value.model || ""; $("#api-key").value = value.apiKeys?.[value.provider] || ""; toggle(); }
function toggle() { const ai = $("#method").value === "ai", dave = $("#provider").value === "dave"; $("#ai").hidden = !ai; $("#key-label").hidden = dave; $("#endpoint-label").hidden = !dave; if (!dave) api.storage.local.get({ organizerSettings: DEFAULTS }).then(x => $("#api-key").value = x.organizerSettings.apiKeys?.[$("#provider").value] || ""); }
$("#method").onchange = toggle; $("#provider").onchange = toggle;
document.querySelector("form").onsubmit = async event => { event.preventDefault(); const old = (await api.storage.local.get({ organizerSettings: DEFAULTS })).organizerSettings; const provider = $("#provider").value, apiKeys = { ...(old.apiKeys || {}) }; if (provider !== "dave") apiKeys[provider] = $("#api-key").value.trim(); await api.storage.local.set({ organizerSettings: { method: $("#method").value, provider, endpoint: $("#endpoint").value.trim() || DEFAULTS.endpoint, model: $("#model").value.trim(), apiKeys } }); $("#status").textContent = "Saved."; };
for (const category of OrganizerCategories.CATEGORIES) { const chip = document.createElement("span"); chip.textContent = category.name; $("#categories").append(chip); } load();
